version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./signals.db:/app/signals.db   # persist SQLite in dev
    restart: unless-stopped

  # Uncomment for production with Postgres:
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: signals
  #     POSTGRES_USER: signals
  #     POSTGRES_PASSWORD: changeme
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # volumes:
  #   pgdata:
